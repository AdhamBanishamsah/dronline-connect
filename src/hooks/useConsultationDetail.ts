
// Import from the .tsx file to ensure consistent types
import { useConsultationDetail } from "./useConsultationDetail.tsx";

// Re-export for backward compatibility
export { useConsultationDetail };
